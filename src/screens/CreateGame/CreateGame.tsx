import { CreateGame } from "../../modules";
import { Background } from "../../components";

export function CreateGameScreen() {
    return (
        <Background>
            <CreateGame />
        </Background>
    );
}
